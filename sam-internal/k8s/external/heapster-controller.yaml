apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    k8s-app: heapster
    name: heapster
    version: v6
  name: heapster
  namespace: kube-system
spec:
  replicas: 1
  selector:
    k8s-app: heapster
    version: v6
  template:
    metadata:
      labels:
        k8s-app: heapster
        version: v6
    spec:
      containers:
      - name: heapster
        image: shared0-samcontrol1-1-prd.eng.sfdc.net:5000/heapster:canary
        imagePullPolicy: Always
        command:
        - /heapster
        - --source=kubernetes:http://shared0-samkubeapi1-1-prd.eng.sfdc.net:8080?inClusterConfig=false
        - --sink=influxdb:http://monitoring-influxdb:8086
