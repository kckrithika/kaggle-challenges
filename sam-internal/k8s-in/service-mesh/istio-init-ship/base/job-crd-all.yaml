---
# Source: istio-init/templates/job-crd-all.yaml
apiVersion: batch/v1
kind: Job
metadata:
  namespace: mesh-control-plane
  name: istio-init-crd-all-%(istioTag)s
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "false"
    spec:
      serviceAccountName: istio-init-service-account
      containers:
      - name: istio-init-crd-all
        image: "%(istioHub)s/kubectl:%(istioTag)s"
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: 100m
            memory: 200Mi
          requests:
            cpu: 10m
            memory: 50Mi
          
        volumeMounts:
        - name: crd-all
          mountPath: /etc/istio/crd-all
          readOnly: true
        command: ["kubectl",  "apply", "-f", "/etc/istio/crd-all/crd-all.gen.yaml"]
      volumes:
      - name: crd-all
        configMap:
          name: istio-crd-all
      restartPolicy: OnFailure
