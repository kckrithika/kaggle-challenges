apiVersion: batch/v1
kind: Job
metadata:
  name: istio-init-crd-mixer-%(istioTag)s
  namespace: mesh-control-plane
spec:
  template:
    spec:
      nodeSelector:
        pool: mcpIstioConfig.istioEstate