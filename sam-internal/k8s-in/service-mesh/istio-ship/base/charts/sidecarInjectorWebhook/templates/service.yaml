apiVersion: v1
kind: Service
metadata:
  labels:
    istio: sidecar-injector
  name: istio-sidecar-injector
  namespace: mesh-control-plane
spec:
  ports:
  - port: 443
  selector:
    istio: sidecar-injector
