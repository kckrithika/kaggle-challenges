apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: sdn-aws-controller
  name: sdn-aws-controller
  namespace: sam-system
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        name: sdn-aws-controller
    spec:
      containers:
      - command:
        - /sdn/sdn-aws-controller
        - --archiveSvcEndpoint=https://ops0-piperepo1-1-cdu.ops.sfdc.net/tnrp/content_repo/0/archive
        - --livenessProbePort=9122
        - --ipamPullInterval=30s
        - --archiveSvcPullInterval=30s
        - --rootPath=/etc/pki_service
        - --userName=kubernetes
        - --pkiServerServiceName=k8s-server
        - --pkiClientServiceName=k8s-client
        - --awsRegion=ap-southeast-2
        - --awsAZ=ap-southeast-2c
        - --ddi=https://ddi-api-crz.data.sfdc.net/
        - --keyfile=/etc/pki_service/platform/platform-client/keys/platform-client-key.pem
        - --certfile=/etc/pki_service/platform/platform-client/certificates/platform-client.pem
        - --cafile=/etc/pki_service/ca/cabundle.pem
        - --hostsConfigFile=/sfdchosts/hosts.json
        - --log_dir=/var/logs/sdn/
        - --logtostderr=false
        - --alsologtostderr=true
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-cdu.data.sfdc.net/tnrp/sdn/hypersdn:v-0000649-00946749
        livenessProbe:
          httpGet:
            path: /liveness-probe
            port: 9122
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
        name: sdn-aws-controller
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /var/logs/sdn/
          name: sdnlogs
      hostNetwork: true
      nodeSelector:
        master: "false"
        pool: cdu-sdn_control
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - hostPath:
          path: /var/logs/sdn/
        name: sdnlogs
