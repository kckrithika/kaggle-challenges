apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-apiserverlb
  name: watchdog-apiserverlb
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-apiserverlb
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-apiserverlb
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=APISERVERLB
        - -watchdogFrequency=60s
        - -alertThreshold=5m
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-hnd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -emailFrequency=12h
        image: ops0-artifactrepo1-0-hnd.data.sfdc.net/tnrp/sam/hypersam:sam-0001622-cbc44617
        name: watchdog-apiserverlb
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: hnd-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
