apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    pcn: deploy
  name: madkubserver
  namespace: sam-system
spec:
  minReadySeconds: 45
  replicas: 1
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      sam.data.sfdc.net/owner: sam
      service: madkubserver
  template:
    metadata:
      labels:
        sam.data.sfdc.net/owner: sam
        service: madkubserver
    spec:
      containers:
      - args:
        - /sam/madkub-server
        - --listen
        - 0.0.0.0:32007
        - -d
        - --maddog-endpoint
        - https://10.168.192.190:8443
        - --kubeconfig
        - $(KUBECONFIG)
        - --client-cert
        - /etc/gcp_certs/tls.crt
        - --client-key
        - /etc/gcp_certs/tls.key
        - --maddog-server-ca
        - /etc/pki_service/ca/security-ca.pem
        - --cert-folder
        - /certs/
        - --token-folder
        - /tokens/
        - --service-hostname
        - $(MADKUBSERVER_SERVICE_HOST)
        - --funnel-endpoint
        - ""
        - --kingdom
        - eks
        - --estate
        - eks-us-east2-sam2-samtest
        - --dns-domain
        - samtest.core.test.us-central1.gcp.sfdc.net
        - --service-hostname
        - madkubserver.sam-system.svc
        env:
        - name: SFDCLOC_PCN_KINGDOM
          value: eks
        - name: SFDCLOC_PCN_ESTATE
          value: eks-us-east2-sam2-samtest
        - name: KUBECONFIG
          value: ""
        image: ops0-artifactrepo2-0-xrd.slb.sfdc.net/docker-gcp/dva/sam/madkub:108-f011ff42f384e0d22ea5b916ee2f3c2146850b2f
        livenessProbe:
          httpGet:
            path: /healthz
            port: 32007
            scheme: HTTPS
          initialDelaySeconds: 30
          periodSeconds: 10
        name: madkubserver
        ports:
        - containerPort: 3000
        volumeMounts:
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /tokens
          name: tokens
        - mountPath: /etc/pki_service/
          name: pki
        - mountPath: /etc/gcp_certs/
          name: tls
      - args:
        - /sam/madkub-client
        - --madkub-endpoint
        - UNUSED
        - --maddog-endpoint
        - https://10.168.192.190:8443
        - --maddog-server-ca
        - /maddog-certs/ca/security-ca.pem
        - --madkub-server-ca
        - /maddog-certs/ca/cacerts.pem
        - --token-folder
        - /tokens/
        - --refresher
        - --refresher-token-grace-period
        - 30s
        - --funnel-endpoint
        - ""
        - --kingdom
        - eks
        - --ca-folder
        - /maddog-certs/ca
        - --cert-folders
        - madkubInternalCert:/certs/
        env:
        - name: MADKUB_NODENAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: MADKUB_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MADKUB_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: ops0-artifactrepo2-0-xrd.slb.sfdc.net/docker-gcp/dva/sam/madkub:108-f011ff42f384e0d22ea5b916ee2f3c2146850b2f
        name: madkub-refresher
        resources: {}
        volumeMounts:
        - mountPath: /certs
          name: datacerts
        - mountPath: /tokens
          name: tokens
        - mountPath: /maddog-certs/
          name: pki
      hostNetwork: false
      nodeSelector: {}
      restartPolicy: Always
      serviceAccount: sam-stack-edit
      serviceAccountName: sam-stack-edit
      volumes:
      - hostPath:
          path: /etc/kubernetes/kubeconfig
        name: kubeconfig
      - hostPath:
          path: /etc/pki_service
        name: pki
      - emptyDir:
          medium: Memory
        name: datacerts
      - emptyDir:
          medium: Memory
        name: tokens
      - name: tls
        secret:
          secretName: madkubserver-cert
