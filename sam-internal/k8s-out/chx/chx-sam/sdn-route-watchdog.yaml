apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: sdn-route-watchdog
  name: sdn-route-watchdog
  namespace: sam-system
spec:
  minReadySeconds: 180
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        apptype: monitoring
        name: sdn-route-watchdog
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sdn/sdn-route-watchdog
        - --funnelEndpoint=ajna0-funnel1-0-chx.data.sfdc.net:80
        - --archiveSvcEndpoint=https://ops-piperepo1-0-chx.data.sfdc.net/tnrp/content_repo/0/archive
        - --momCollectorEndpoint=
        - --smtpServer=ops-mta1-4-chx.ops.sfdc.net:25
        - --sender=sdn-alerts@salesforce.com
        - --recipient=sdn@salesforce.com
        - --emailFrequency=12h
        - --watchdogFrequency=180s
        - --alertThreshold=300s
        - --livenessProbePort=9104
        env:
        - name: KUBECONFIG
          value: /config/kubeconfig
        image: ops-artifactrepo1-0-chx.data.sfdc.net/tnrp/sdn/hypersdn:v-0000475-9a7fd185
        livenessProbe:
          httpGet:
            path: /liveness-probe
            port: 9104
          initialDelaySeconds: 5
          periodSeconds: 20
          timeoutSeconds: 5
        name: sdn-route-watchdog
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: chx-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: config
