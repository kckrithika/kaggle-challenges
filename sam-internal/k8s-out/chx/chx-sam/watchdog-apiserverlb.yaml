apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-apiserverlb
  name: watchdog-apiserverlb
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-apiserverlb
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-apiserverlb
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=APISERVERLB
        - -watchdogFrequency=60s
        - -alertThreshold=5m
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-chx.data.sfdc.net:80
        - --config=/config/watchdog.json
        - -emailFrequency=12h
        image: ops-artifactrepo1-0-chx.data.sfdc.net/tnrp/sam/hypersam:sam-0001548-81d3b9bd
        name: watchdog-apiserverlb
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: chx-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
