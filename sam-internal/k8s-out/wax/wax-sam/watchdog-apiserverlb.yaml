apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-apiserverlb
  name: watchdog-apiserverlb
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-apiserverlb
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-apiserverlb
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=APISERVERLB
        - -watchdogFrequency=60s
        - -alertThreshold=5m
        - -timeout=2s
        - -funnelEndpoint=mandm-funnel-wax.data.sfdc.net:8080
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -emailFrequency=12h
        image: ops-artifactrepo1-0-wax.data.sfdc.net/tnrp/sam/hypersam:sam-0001622-cbc44617
        name: watchdog-apiserverlb
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: wax-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
