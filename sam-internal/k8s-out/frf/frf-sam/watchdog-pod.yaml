apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-pod
  name: watchdog-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-pod
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-pod
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=POD
        - -watchdogFrequency=60s
        - -alertThreshold=1h
        - -maxUptimeSampleSize=5
        - -podNamespacePrefixBlacklist=sam-watchdog
        - -timeout=2s
        - -funnelEndpoint=mandm-funnel-frf1.data.sfdc.net:8080
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -emailFrequency=24h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-frf.data.sfdc.net/tnrp/sam/hypersam:sam-0001548-81d3b9bd
        name: watchdog-pod
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: frf-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: watchdog
        name: config
