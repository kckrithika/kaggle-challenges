apiVersion: v1
kind: Service
metadata:
  annotations:
    cloud.google.com/load-balancer-type: Internal
  labels:
    app: topology-svc-lb
    pcn: deploy
  name: topology-svc-lb
  namespace: topology-svc
spec:
  externalTrafficPolicy: Cluster
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: topology-svc
  sessionAffinity: None
  type: LoadBalancer
