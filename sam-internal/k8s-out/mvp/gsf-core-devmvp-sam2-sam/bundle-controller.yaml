apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: bundlecontroller
    pcn: deploy
    sam.data.sfdc.net/owner: sam
  name: bundlecontroller
spec:
  replicas: 1
  selector:
    matchLabels:
      name: bundlecontroller
  template:
    metadata:
      labels:
        apptype: control
        name: bundlecontroller
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/bundlecontroller
        - --config=/config/bundle-controller-config.json
        - --funnelEndpoint=ajna0-funnel1-0-mvp.data.sfdc.net:80
        env:
        - name: SFDCLOC_PCN_KINGDOM
          value: mvp
        - name: SFDCLOC_PCN_ESTATE
          value: gsf-core-devmvp-sam2-sam
        - name: KUBECONFIG
          value: ""
        image: ops0-artifactrepo2-0-xrd.slb.sfdc.net/dva/sam/hypersam:2642-2ba92e7906255d862adce32b13b05bc343975210
        livenessProbe:
          httpGet:
            path: /healthz
            port: 21546
          initialDelaySeconds: 30
          periodSeconds: 5
        name: bundle-controller
        volumeMounts:
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: gsf-core-devmvp-sam2-sam
      serviceAccount: sam-stack-edit
      serviceAccountName: sam-stack-edit
      volumes:
      - configMap:
          name: bundle-controller
        name: config
