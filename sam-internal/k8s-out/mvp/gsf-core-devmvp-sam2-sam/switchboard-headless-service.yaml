apiVersion: v1
kind: Service
metadata:
  labels:
    app: switchboard
    pcn: deploy
  name: switchboard
  namespace: service-mesh
spec:
  clusterIP: None
  ports:
  - port: 15001
    protocol: TCP
    targetPort: 15001
  selector:
    sam_app: switchboard-mvp
    sam_function: switchboard
  type: ClusterIP
