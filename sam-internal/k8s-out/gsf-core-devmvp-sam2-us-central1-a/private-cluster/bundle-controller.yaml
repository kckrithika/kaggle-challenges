apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: bundlecontroller
    pcn: deploy
    sam.data.sfdc.net/owner: sam
  name: bundlecontroller
spec:
  replicas: 1
  selector:
    matchLabels:
      name: bundlecontroller
  template:
    metadata:
      labels:
        apptype: control
        name: bundlecontroller
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/bundlecontroller
        - --config=/config/bundle-controller-config.json
        - --funnelEndpoint=ajna0-funnel1-0-gsf-core-devmvp-sam2-us-central1-a.data.sfdc.net:80
        env:
        - name: SFDCLOC_PCN_KINGDOM
          value: gsf-core-devmvp-sam2-us-central1-a
        - name: SFDCLOC_PCN_ESTATE
          value: private-cluster
        image: ops0-artifactrepo1-0-gsf-core-devmvp-sam2-us-central1-a.data.sfdc.net/tnrp/sam/hypersam:sam-0002417-c81b1d32
        name: bundle-controller
        volumeMounts:
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        pool: private-cluster
      volumes:
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: bundle-controller
        name: config
