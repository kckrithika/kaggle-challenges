apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: watchdog-rbac
  name: watchdog-rbac
spec:
  selector:
    matchLabels:
      name: watchdog-rbac
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-rbac
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=RBAC
        - -watchdogFrequency=180s
        - -alertThreshold=1h
        - -maxUptimeSampleSize=5
        - -timeout=2s
        - -funnelEndpoint=mandm-funnel-par1.data.sfdc.net:8080
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -emailFrequency=24h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-par.data.sfdc.net/tnrp/sam/hypersam:sam-0001641-fafe532f
        name: watchdog-rbac
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: watchdog
        name: config
