apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRole
metadata:
  labels:
    app: zoobernetes
  name: zoobernetes-serviceentries-readwrite
  namespace: discovery
rules:
- apiGroups:
  - '*'
  resources:
  - namespaces
  - services
  - serviceentries
  verbs:
  - '*'
