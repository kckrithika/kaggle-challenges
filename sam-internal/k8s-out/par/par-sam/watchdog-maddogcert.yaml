apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: watchdog-maddogcert
    sam.data.sfdc.net/owner: sam
  name: watchdog-maddogcert
spec:
  selector:
    matchLabels:
      apptype: monitoring
      daemonset: "true"
      name: watchdog-maddogcert
      sam.data.sfdc.net/owner: sam
  template:
    metadata:
      labels:
        apptype: monitoring
        daemonset: "true"
        name: watchdog-maddogcert
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=MADDOGCERT
        - -watchdogFrequency=10m
        - -alertThreshold=30m
        - -emailFrequency=24h
        - -watchDogKind=DaemonSet
        - -timeout=2s
        - -funnelEndpoint=mandm-funnel-par1.data.sfdc.net:8080
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-par.data.sfdc.net/dva/sam/hypersam:2633-e508caaf95457f35c52db83edd42ac2586078c24
        name: watchdog-maddogcert
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
      hostNetwork: true
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /data/certs
        name: certs
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 25%
    type: RollingUpdate
