apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-node-controller
    sam.data.sfdc.net/owner: sam
  name: watchdog-node-controller
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-node-controller
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-node-controller
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=NODECONTROLLER
        - -watchdogFrequency=10s
        - -alertThreshold=300s
        - -watchDogKind=Deployment
        - -funnelEndpoint=ajna0-funnel1-0-xrd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -timeout=2s
        - -emailFrequency=336h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/dva/sam/hypersam:2828-66a213039cc3d696cf5b35fe6275bf76b841c5a8
        name: watchdog-node-controller
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
