apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: slb-iface-processor
  name: slb-iface-processor
  namespace: sam-system
spec:
  minReadySeconds: 30
  template:
    metadata:
      labels:
        apptype: control
        daemonset: "true"
        name: slb-iface-processor
      namespace: sam-system
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: pool
                operator: In
                values:
                - xrd-slb
      containers:
      - command:
        - /sdn/slb-iface-processor
        - --configDir=/host/data/slb/config/slb-iface-processor
        - --control.sentinelExpiration=120s
        - --period=5s
        - --metricsEndpoint=ajna0-funnel1-0-xrd.data.sfdc.net:80
        - --log_dir=/host/data/slb/logs/slb-iface-processor
        - --hostsConfigFile=/sfdchosts/hosts.json
        - --readVipsFromIpvs=true
        - --client.serverPort=12086
        - --client.serverInterface=lo
        - --client.socketDir=/host/data/slb/config/slb-iface-processor
        - --client.dialSocket=true
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/tnrp/sdn/hypersdn:v-0001131-d6252aec
        name: slb-iface-processor
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /host/data/slb
          name: var-slb-volume
        - mountPath: /host/data/slb/config/slb-iface-processor
          name: var-config-volume
        - mountPath: /host/data/slb/logs/slb-iface-processor
          name: var-logs-volume
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /host/sbin
          name: sbin-volume
      - command:
        - /sdn/slb-config-processor
        - --configDir=/host/data/slb/config/slb-iface-processor
        - --period=1800s
        - --podPhaseCheck=true
        - --namespace=
        - --podstatus=running
        - --subnet=10.229.32.0/22
        - --k8sapiserver=
        - --serviceList=
        - --useVipLabelToSelectSvcs=true
        - --metricsEndpoint=ajna0-funnel1-0-xrd.data.sfdc.net:80
        - --log_dir=/host/data/slb/logs/slb-iface-processor
        - --sleepTime=100ms
        - --processKnEConfigs=true
        - --kneConfigDir=/var/slb/kneconfigs/xrd
        - --kneDomainName=slb.sfdc.net
        - --livenessProbePort=12087
        - --shouldRemoveConfig=true
        - --hostsConfigFile=/sfdchosts/hosts.json
        - --proxySelectorLabelValue=slb-nginx-config-b
        - --hostnameOverride=$(NODE_NAME)
        - --control.configProcSentinel=/host/data/slb/config/slb-iface-processor/slb-config-proc.sentinel
        - --alwaysPopulateRealServers=true
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/tnrp/sdn/hypersdn:v-0001131-d6252aec
        livenessProbe:
          httpGet:
            path: /liveness-probe
            port: 12087
          initialDelaySeconds: 600
          periodSeconds: 30
          timeoutSeconds: 5
        name: slb-config-processor
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /host/data/slb
          name: var-slb-volume
        - mountPath: /host/data/slb/config/slb-iface-processor
          name: var-config-volume
        - mountPath: /host/data/slb/logs/slb-iface-processor
          name: var-logs-volume
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
      - command:
        - /sdn/slb-cleanup
        - --period=1800s
        - --logsMaxAge=1h
        - --filesDirToCleanup=/host/data/slb/config/slb-iface-processor
        - --shouldSkipServiceRecords=true
        - --shouldNotDeleteAllFiles=true
        - --log_dir=/host/data/slb/logs/slb-iface-processor
        - --shouldSkipSlbBlock=true
        - --skipFilesWithSuffix=.sock
        - --maxDeleteFileCount=20
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/tnrp/sdn/hypersdn:v-0001131-d6252aec
        name: slb-cleanup-config-processor
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /host/data/slb
          name: var-slb-volume
        - mountPath: /host/data/slb/config/slb-iface-processor
          name: var-config-volume
        - mountPath: /host/data/slb/logs/slb-iface-processor
          name: var-logs-volume
      - command:
        - /sdn/slb-node-api
        - --port=12086
        - --configDir=/host/data/slb/config/slb-iface-processor
        - --log_dir=/host/data/slb/logs/slb-iface-processor
        - --netInterface=lo
        - --checkSentinel=true
        - --control.configProcSentinel=/host/data/slb/config/slb-iface-processor/slb-config-proc.sentinel
        - --control.sentinelExpiration=1800s
        - --listenOnSocket=true
        - --readOnly=false
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/tnrp/sdn/hypersdn:v-0001131-d6252aec
        name: slb-node-api
        volumeMounts:
        - mountPath: /host/data/slb
          name: var-slb-volume
        - mountPath: /host/data/slb/logs/slb-iface-processor
          name: var-logs-volume
      - command:
        - /sdn/slb-cleanup
        - --period=1800s
        - --logsMaxAge=48h
        - --log_dir=/host/data/slb/logs/slb-iface-processor/cleanup
        - --filesDirToCleanup=/host/data/slb/logs/slb-iface-processor
        - --shouldSkipServiceRecords=false
        - --shouldNotDeleteAllFiles=false
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-xrd.data.sfdc.net/tnrp/sdn/hypersdn:v-0001131-d6252aec
        name: slb-cleanup
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /host/data/slb
          name: var-slb-volume
        - mountPath: /host/data/slb/config/slb-iface-processor
          name: var-config-volume
        - mountPath: /host/data/slb/logs/slb-iface-processor
          name: var-logs-volume
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /host/data/slb/logs/slb-iface-processor/cleanup
          name: var-cleanup-logs-volume
      hostNetwork: true
      volumes:
      - hostPath:
          path: /data/slb
        name: var-slb-volume
      - hostPath:
          path: /data/slb/config/slb-iface-processor
        name: var-config-volume
      - hostPath:
          path: /data/slb/logs/slb-iface-processor
        name: var-logs-volume
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /sbin
        name: sbin-volume
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /data/slb/logs/slb-iface-processor/cleanup
        name: var-cleanup-logs-volume
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 20%
    type: RollingUpdate
