apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-kuberesources
  name: watchdog-kuberesources
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-kuberesources
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-kuberesources
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=KUBERESOURCES
        - -watchdogFrequency=120s
        - -alertThreshold=1h
        - -maxUptimeSampleSize=5
        - -shouldBatchMetrics=true
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -emailFrequency=24h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001585-fdaa5a39
        name: watchdog-kuberesources
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: watchdog
        name: config
