apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-artifactory
    sam.data.sfdc.net/owner: sam
  name: watchdog-artifactory
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-artifactory
  template:
    metadata:
      labels:
        apptype: comparision
        name: watchdog-artifactory
        sam.data.sfdc.net/owner: sam
    spec:
      containers:
      - command:
        - /sam/watchdog
        - --role=ARTIFACTORY
        - --emailFrequency=24h
        - --watchdogFrequency=30m
        - --alertThreshold=1h
        - --enableEmailPerCheckerInstance=true
        - --recipient=sam@salesforce.com
        - --sender=sam@salesforce.com
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -timeout=120s
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sam/hypersam:2662-7d92133983fc3617ead646326f8b83d3225e5bb8
        name: watchdog-artifactory
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /data/certs
        name: certs
