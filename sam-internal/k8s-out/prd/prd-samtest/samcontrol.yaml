apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: samcontrol
  name: samcontrol
spec:
  replicas: 1
  selector:
    matchLabels:
      name: samcontrol
  template:
    metadata:
      labels:
        apptype: control
        name: samcontrol
    spec:
      containers:
      - command:
        - /sam/sam-controller
        - --dockerregistry=ops0-artifactrepo2-0-prd.data.sfdc.net
        - --funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --v=3
        - --logtostderr=true
        - --config=/config/samcontrol.json
        - --deletionEnabled=true
        - --deletionPercentageThreshold=10
        - --statefulAppEnabled=true
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig
        image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/cbatra/hypersam:20170906_143330.3aefdeb.dirty.cbatra-ltm1
        name: sam-controller
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: samcontrol
        name: config
