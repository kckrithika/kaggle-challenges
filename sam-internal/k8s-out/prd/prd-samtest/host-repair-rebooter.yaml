apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: host-repair-rebooter
  name: host-repair-rebooter
spec:
  replicas: 1
  selector:
    matchLabels:
      name: host-repair-rebooter
  template:
    metadata:
      labels:
        apptype: control
        name: host-repair-rebooter
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/host-repair-rebooter
        - --config=/config/host-repair-rebooter.json
        - -v=0
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sam/hypersam:2649-46a5ccc614e9f2cefc09080a40192f5a1eccb5d1
        name: host-repair-rebooter
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: host-repair-rebooter
        name: config
