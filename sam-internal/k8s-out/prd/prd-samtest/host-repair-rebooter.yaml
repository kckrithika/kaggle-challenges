apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: host-repair-rebooter
  name: host-repair-rebooter
spec:
  replicas: 1
  selector:
    matchLabels:
      name: host-repair-rebooter
  template:
    metadata:
      labels:
        apptype: control
        name: host-repair-rebooter
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/host-repair-rebooter
        - --config=/config/host-repair-rebooter.json
        - -v=0
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0002428-23701a48
        name: host-repair-rebooter
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: host-repair-rebooter
        name: config
