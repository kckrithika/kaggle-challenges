apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: slb-tcpdump-ipvs
    sam.data.sfdc.net/owner: slb
  name: slb-tcpdump-ipvs
  namespace: sam-system
spec:
  selector:
    matchLabels:
      daemonset: "true"
      name: slb-tcpdump-ipvs
      sam.data.sfdc.net/owner: slb
  template:
    metadata:
      labels:
        daemonset: "true"
        name: slb-tcpdump-ipvs
        sam.data.sfdc.net/owner: slb
    spec:
      containers:
      - command:
        - /sdn/slb-tcpdump
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/slb/hyperslb:2097-be25220695b853ead3d3de1288f56b53cdb95bd1
        name: slb-tcpdump-ipvs
        resources:
          limits:
            cpu: "0.5"
            memory: 300Mi
            sam.sfdc.net/ip-address: "1"
          requests:
            cpu: "0.5"
            memory: 300Mi
            sam.sfdc.net/ip-address: "1"
        volumeMounts:
        - mountPath: /config
          name: config
      dnsPolicy: Default
      nodeSelector:
        slb-service: slb-ipvs
      terminationGracePeriodSeconds: 5
      volumes:
      - configMap:
          name: slb-tcpdump-ipvs
        name: config
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 25%
    type: RollingUpdate
