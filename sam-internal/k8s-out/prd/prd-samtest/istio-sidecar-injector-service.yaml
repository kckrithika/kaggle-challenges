apiVersion: v1
kind: Service
metadata:
  labels:
    istio: sidecar-injector
  name: istio-sidecar-injector
  namespace: service-mesh
spec:
  ports:
  - name: sidecar-injector-port
    port: 15009
    targetPort: 15009
  selector:
    istio: sidecar-injector
