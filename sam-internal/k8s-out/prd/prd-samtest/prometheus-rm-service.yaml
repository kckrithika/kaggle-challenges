apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: prometheus-rm
    sam.data.sfdc.net/owner: sam
  name: prometheus-rm-svc
  namespace: sam-system
spec:
  ports:
  - name: prometheus-rm-port
    nodePort: 38001
    port: 80
    protocol: TCP
    targetPort: 9090
  selector:
    name: prometheus-rm
  type: NodePort
