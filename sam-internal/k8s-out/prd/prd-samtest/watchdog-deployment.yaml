apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-deployment
    sam.data.sfdc.net/owner: sam
  name: watchdog-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-deployment
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-deployment
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=DEPLOYMENT
        - -watchdogFrequency=10s
        - -alertThreshold=1h
        - -watchDogKind=Deployment
        - -emailFrequency=10000h
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sam/hypersam:2640-51b6be1dab8ab91922b88dc4c23d001b9d647feb
        name: watchdog-deployment
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
