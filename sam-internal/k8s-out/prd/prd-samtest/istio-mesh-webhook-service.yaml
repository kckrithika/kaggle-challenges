apiVersion: v1
kind: Service
metadata:
  name: istio-mesh-webhook
  namespace: mesh-control-plane
spec:
  ports:
  - name: https
    port: 443
    targetPort: 10443
  selector:
    app: istio-mesh-webhook
