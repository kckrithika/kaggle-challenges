apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
    slb.sfdc.net/name: prometheus
  name: prometheus-svc
  namespace: sam-system
spec:
  ports:
  - name: prometheus-port
    nodePort: 38000
    port: 9090
    protocol: TCP
    targetPort: 9090
  selector:
    name: prometheus
  type: NodePort
