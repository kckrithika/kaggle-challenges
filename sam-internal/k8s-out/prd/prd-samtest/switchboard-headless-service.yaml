apiVersion: v1
kind: Service
metadata:
  labels:
    app: switchboard
    some_fake_label: "2"
  name: switchboard
  namespace: service-mesh
spec:
  clusterIP: None
  ports:
  - port: 15001
    protocol: TCP
    targetPort: 15001
  selector:
    sam_app: switchboard
    sam_function: switchboard
  type: ClusterIP
