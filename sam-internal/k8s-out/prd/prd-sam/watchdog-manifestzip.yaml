apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-manifestzip
    sam.data.sfdc.net/owner: sam
  name: watchdog-manifestzip
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-manifestzip
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-manifestzip
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=MANIFESTZIP
        - -watchdogFrequency=7m
        - -alertThreshold=10s
        - -emailFrequency=336h
        - -watchDogKind=Deployment
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -timeout=2s
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sam/hypersam:2814-bdc65579e01f6a124d636d2cb837cb7e6d4afd6f
        name: watchdog-manifestzip
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /var/token
          name: token
          readOnly: true
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
      hostNetwork: true
      nodeSelector:
        pool: prd-sam
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - name: token
        secret:
          secretName: git-token
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /data/certs
        name: certs
