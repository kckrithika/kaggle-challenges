apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-sdp
    sam.data.sfdc.net/owner: sam
  name: watchdog-sdp
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-sdp
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-sdp
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=SDP
        - -watchdogFrequency=10s
        - -alertThreshold=300s
        - -emailFrequency=24h
        - -watchDogKind=Deployment
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0002135-e7f3c512
        name: watchdog-sdp
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
      hostNetwork: true
      nodeSelector:
        pool: prd-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
