apiVersion: v1
kind: Service
metadata:
  labels:
    app: ceph-metrics
    slb.sfdc.net/name: ceph-metrics
  name: ceph-metrics
  namespace: legostore
spec:
  ports:
  - name: ceph-metrics
    nodePort: 38001
    port: 8001
    protocol: TCP
    targetPort: 8001
  selector:
    app: ceph-metrics
  type: NodePort
