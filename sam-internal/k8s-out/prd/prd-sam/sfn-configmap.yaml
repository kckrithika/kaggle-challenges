apiVersion: v1
data:
  sfn-selectors.yaml: |
    pods:
        matchExpressions:
            - {key: cloud, operator: In, values: [storage]}
    nodes:
        matchExpressions:
            - {key: pool, operator: In, values: ["prd-sam_ceph", "prd-sam_sfstore"]}
    persistentvolumes:
        matchExpressions:
            - {key: pool, operator: In, values: ["prd-sam_ceph", "prd-sam_sfstore"]}
    persistentvolumeclaims:
        matchExpressions:
            - {key: daemon, operator: In, values: ["mon", "osd"]}
    statefulsets:
        matchExpressions:
            - {key: daemon, operator: In, values: ["mon", "osd"]}
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: sfn-config
  namespace: sam-system
