apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: temp-secret-samcontrol-deployer
  name: temp-secret-samcontrol-deployer
  namespace: sam-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        apptype: control
        name: temp-secret-samcontrol-deployer
    spec:
      containers:
      - command:
        - /sam/samcontrol-deployer
        - --config=/config/tempsecretsamcontroldeployer.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001747-8c6b4886
        livenessProbe:
          httpGet:
            path: /
            port: 9099
          initialDelaySeconds: 2
          periodSeconds: 10
          timeoutSeconds: 10
        name: temp-secret-samcontrol-deployer
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: temp-secret-samcontrol-deployer
        name: config
