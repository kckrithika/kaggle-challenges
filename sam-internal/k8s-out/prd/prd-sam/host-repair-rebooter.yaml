apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: host-repair-rebooter
  name: host-repair-rebooter
spec:
  replicas: 1
  selector:
    matchLabels:
      name: host-repair-rebooter
  template:
    metadata:
      labels:
        apptype: control
        name: host-repair-rebooter
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/host-repair-rebooter
        - --config=/config/host-repair-rebooter.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sam/hypersam:2884-769ac132f739c69e8f1f603ca3316a9ab3b70d1e
        name: host-repair-rebooter
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /data/certs
          name: certs
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: host-repair-rebooter
        name: config
