apiVersion: v1
kind: Service
metadata:
  name: istio-routing-webhook
  namespace: mesh-control-plane
spec:
  ports:
  - name: https
    port: 443
    targetPort: 10443
  selector:
    app: istio-routing-webhook
