apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: slb-canary
    sam.data.sfdc.net/owner: slb
  name: slb-canary
  namespace: sam-system
spec:
  minReadySeconds: 30
  replicas: 2
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        name: slb-canary
        sam.data.sfdc.net/owner: slb
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sdn/slb-canary-service
        - --serviceName=slb-canary-service
        - --log_dir=/host/data/slb/logs/
        - --ports=9111
        - --tlsPorts=9443
        - --privateKey=/var/slb/canarycerts/server.key
        - --publicKey=/var/slb/canarycerts/sam.crt
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sdn/hypersdn:v-0000980-6a830502
        name: slb-canary
        securityContext:
          capabilities:
            add:
            - ALL
          privileged: true
        volumeMounts:
        - mountPath: /host/data/slb/logs/
          name: var-logs-volume
      hostNetwork: true
      nodeSelector:
        pool: prd-sam
      volumes:
      - hostPath:
          path: /data/slb/logs/
        name: var-logs-volume
