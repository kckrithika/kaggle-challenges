apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-samsql
  name: watchdog-samsql
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-samsql
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-samsql
    spec:
      containers:
      - command:
        - /sam/watchdog
        - --role=MYSQL
        - --sqlDbUsername=root
        - --sqlDbPasswordFile=/var/secrets/mysql.txt
        - --sqlDbHostname=mysql.csc-sam.prd-sam.prd.slb.sfdc.net
        - --sqlK8sResourceDbName=sam_kube_resource
        - --sqlDbPort=3306
        - --sqlQueryFile=/var/queries/watchdog-samsql-queries.jsonnet
        - --deployer-emailFrequency=24h
        - -v=5
        - --alsologtostderr
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001838-2b71386f
        name: watchdog-samsql
        volumeMounts:
        - mountPath: /var/secrets/
          name: mysql
          readOnly: true
        - mountPath: /var/queries/
          name: queries
          readOnly: true
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
        - mountPath: /etc/pki_service
          name: maddog-certs
      nodeSelector:
        pool: prd-sam
      volumes:
      - name: mysql
        secret:
          secretName: mysql
      - configMap:
          name: watchdogsamsqlqueries
        name: queries
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
