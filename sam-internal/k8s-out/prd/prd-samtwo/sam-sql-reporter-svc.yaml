apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: samsqlreporter
    sam.data.sfdc.net/owner: sam
  name: samsqlreporter
  namespace: sam-system
spec:
  ports:
  - name: ssr-port
    port: 80
    protocol: TCP
    targetPort: 64212
  selector:
    name: samsqlreporter
