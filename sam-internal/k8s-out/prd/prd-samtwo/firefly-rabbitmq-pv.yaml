apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    type: local
  name: firefly-rabbitmq-pv-volume
  namespace: firefly
spec:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 100Gi
  hostPath:
    path: /data/firefly
  storageClassName: manual
