apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: firefly-rabbitmq-pv-claim
  namespace: firefly
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: manual
