apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: slb-tcpdump-ipvs
  name: slb-tcpdump-ipvs
  namespace: sam-system
spec:
  selector:
    matchLabels:
      daemonset: "true"
      name: slb-tcpdump-ipvs
  template:
    metadata:
      labels:
        daemonset: "true"
        name: slb-tcpdump-ipvs
    spec:
      containers:
      - command:
        - /sdn/slb-tcpdump
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/slb/hyperslb:2088-7ceff4228fef6177c35815de9fa3800ff546c463
        name: slb-tcpdump-ipvs
        resources:
          limits:
            cpu: "0.5"
            memory: 300Mi
            sam.sfdc.net/ip-address: "1"
          requests:
            cpu: "0.5"
            memory: 300Mi
            sam.sfdc.net/ip-address: "1"
        volumeMounts:
        - mountPath: /config
          name: config
      dnsPolicy: Default
      nodeSelector:
        slb-service: slb-ipvs
      terminationGracePeriodSeconds: 5
      volumes:
      - configMap:
          name: slb-tcpdump-ipvs
        name: config
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 25%
    type: RollingUpdate
