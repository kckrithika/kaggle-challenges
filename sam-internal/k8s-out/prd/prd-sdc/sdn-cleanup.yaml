apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: sdn-cleanup
  name: sdn-cleanup
  namespace: sam-system
spec:
  selector:
    matchLabels:
      apptype: control
      daemonset: "true"
      name: sdn-cleanup
  template:
    metadata:
      labels:
        apptype: control
        daemonset: "true"
        name: sdn-cleanup
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sdn/sdn-cleanup
        - --period=1440m
        - --logsMaxAge=48h
        - --filesDirToCleanup=/var/logs/sdn/
        - --shouldNotDeleteAllFiles=false
        - --log_dir=/var/logs/sdn/
        - --logtostderr=false
        - --alsologtostderr=true
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-dva-rc/dva/sdn/hypersdn:2098-05c0a7dbc2f01f9013a085a721e0d264e95b573d
        name: sdn-cleanup
        resources:
          limits:
            sam.sfdc.net/ip-address: "1"
          requests:
            sam.sfdc.net/ip-address: "1"
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: /var/logs/sdn/
          name: sdnlogs
      volumes:
      - hostPath:
          path: /var/logs/sdn/
        name: sdnlogs
