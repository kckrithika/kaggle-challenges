apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: stateful-revision-cleaner
  name: stateful-revision-cleaner
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: stateful-revision-cleaner
  template:
    metadata:
      labels:
        apptype: control
        name: stateful-revision-cleaner
    spec:
      containers:
      - command:
        - /sam/stateful-revision-cleaner
        - --v=5
        - --k8sapiserver=
        - --namespacesToSkip=sam-watchdog,legostore,sam-system,sf-store
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001730-c7caec88
        name: stateful-revision-cleaner
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
