apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-maddog
  name: watchdog-maddog
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-maddog
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-maddog
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=MADDOG
        - -watchdogFrequency=10s
        - -alertThreshold=300s
        - -madkub-endpoint=https://$(MADKUBSERVER_SERVICE_HOST):32007/healthz
        - -maddog-endpoint=https://all.pkicontroller.pki.blank.prd.prod.non-estates.sfdcsd.net:8443/sfdc/v1/ping
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - -emailFrequency=24h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig
        image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/prabh.singh/hypersam:20170919_230658.51e210d.dirty.prabhsingh-ltm5
        name: watchdog-maddog
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: watchdog
        name: config
