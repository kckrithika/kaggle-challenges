apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-proxy
  name: watchdog-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-proxy
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-proxy
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=K8SPROXY
        - -k8sproxyEndpoint=http://localhost:40000
        - -watchdogFrequency=10s
        - -alertThreshold=300s
        - -emailFrequency=48h
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001343-3b7abea8
        name: watchdog-proxy
        volumeMounts:
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - configMap:
          name: watchdog
        name: config
