apiVersion: v1
kind: Service
metadata:
  name: impersonation-proxy
  namespace: flowsnake
spec:
  ports:
  - name: https
    port: 444
    protocol: TCP
    targetPort: 443
  selector:
    app: impersonation-proxy
  type: ClusterIP
