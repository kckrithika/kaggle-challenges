apiVersion: v1
kind: Service
metadata:
  labels:
    app: metric-streamer
  name: metric-streamer
spec:
  clusterIP: None
  ports:
  - name: http-metrics
    port: 8001
    protocol: TCP
    targetPort: 8001
  selector:
    app: metric-streamer
