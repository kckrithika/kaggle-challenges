apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: watchdog-pod
  name: watchdog-pod
spec:
  replicas: 1
  selector:
    matchLabels:
      name: watchdog-pod
  template:
    metadata:
      labels:
        apptype: monitoring
        name: watchdog-pod
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=POD
        - -watchdogFrequency=60s
        - -alertThreshold=1h
        - -maxUptimeSampleSize=5
        - -podNamespacePrefixBlacklist=sam-watchdog
        - -podNamespacePrefixWhitelist=sam-system
        - -timeout=2s
        - -funnelEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80
        - --config=/config/watchdog.json
        - -emailFrequency=24h
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig
        image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-release-candidate/tnrp/sam/hypersam:sam-0001302-4f86e9c4
        name: watchdog-pod
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /config
          name: config
      hostNetwork: true
      nodeSelector:
        master: "true"
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: watchdog
        name: config
