# This file generated by build.sh.  We have open access here because we rely on evaluate_pr to recompile and check for differences, so only folks who can edit templates can change this folder
users:
  - "*"
