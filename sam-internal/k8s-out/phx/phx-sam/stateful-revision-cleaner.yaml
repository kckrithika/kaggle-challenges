apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: stateful-revision-cleaner
  name: stateful-revision-cleaner
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: stateful-revision-cleaner
  template:
    metadata:
      labels:
        apptype: control
        name: stateful-revision-cleaner
    spec:
      containers:
      - command:
        - /sam/stateful-revision-cleaner
        - --v=5
        - --k8sapiserver=
        - --namespacesToSkip=sam-watchdog,legostore,sam-system,sf-store
        - --hostsConfigFile=/sfdchosts/hosts.json
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-phx.data.sfdc.net/tnrp/sam/hypersam:sam-0001760-c9a04911
        name: stateful-revision-cleaner
        volumeMounts:
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /data/certs
          name: certs
        - mountPath: /kubeconfig
          name: kubeconfig
      hostNetwork: true
      nodeSelector:
        pool: phx-sam
      volumes:
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /data/certs
        name: certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
