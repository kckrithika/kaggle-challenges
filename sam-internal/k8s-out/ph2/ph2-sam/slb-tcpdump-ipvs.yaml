apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: slb-tcpdump-ipvs
  name: slb-tcpdump-ipvs
  namespace: sam-system
spec:
  selector:
    matchLabels:
      daemonset: "true"
      name: slb-tcpdump-ipvs
  template:
    metadata:
      labels:
        daemonset: "true"
        name: slb-tcpdump-ipvs
    spec:
      containers:
      - command:
        - /sdn/slb-tcpdump
        - --tcpdump.pollinterval=15m
        image: ops0-artifactrepo1-0-ph2.data.sfdc.net/dva/slb/hyperslb:2155-95ed23888661e74b3460407a65290d2106501a00
        name: slb-tcpdump-ipvs
        resources:
          limits:
            cpu: "0.5"
            memory: 300Mi
          requests:
            cpu: "0.5"
            memory: 300Mi
        volumeMounts:
        - mountPath: /config
          name: config
      dnsPolicy: Default
      nodeSelector:
        slb-service: slb-ipvs
      terminationGracePeriodSeconds: 5
      volumes:
      - configMap:
          name: slb-tcpdump-ipvs
        name: config
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 25%
    type: RollingUpdate
