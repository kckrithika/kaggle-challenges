apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  labels:
    name: watchdog-maddogcert
    sam.data.sfdc.net/owner: sam
  name: watchdog-maddogcert
spec:
  selector:
    matchLabels:
      apptype: monitoring
      daemonset: "true"
      name: watchdog-maddogcert
      sam.data.sfdc.net/owner: sam
  template:
    metadata:
      labels:
        apptype: monitoring
        daemonset: "true"
        name: watchdog-maddogcert
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/watchdog
        - -role=MADDOGCERT
        - -watchdogFrequency=10m
        - -alertThreshold=30m
        - -emailFrequency=336h
        - -watchDogKind=DaemonSet
        - -funnelEndpoint=ajna0-funnel1-0-yul.data.sfdc.net:80
        - --config=/config/watchdog.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - -timeout=2s
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-yul.data.sfdc.net/dva/sam/hypersam:2814-bdc65579e01f6a124d636d2cb837cb7e6d4afd6f
        name: watchdog-maddogcert
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
      hostNetwork: true
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - configMap:
          name: watchdog
        name: config
      - hostPath:
          path: /data/certs
        name: certs
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 25%
    type: RollingUpdate
