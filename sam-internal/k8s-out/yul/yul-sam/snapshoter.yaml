apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: snapshoter
    sam.data.sfdc.net/owner: sam
  name: snapshoter
  namespace: sam-system
spec:
  replicas: 1
  selector:
    matchLabels:
      name: snapshoter
  template:
    metadata:
      labels:
        apptype: control
        name: snapshoter
        sam.data.sfdc.net/owner: sam
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sam/snapshoter
        - --config=/config/snapshoter.json
        - --hostsConfigFile=/sfdchosts/hosts.json
        - --v=4
        - --alsologtostderr
        env:
        - name: KUBECONFIG
          value: /kubeconfig/kubeconfig-platform
        image: ops0-artifactrepo1-0-yul.data.sfdc.net/tnrp/sam/hypersam:sam-0002336-6a8184c1
        livenessProbe:
          httpGet:
            path: /
            port: 9095
          initialDelaySeconds: 20
          periodSeconds: 20
          timeoutSeconds: 20
        name: snapshoter
        volumeMounts:
        - mountPath: /etc/pki_service
          name: maddog-certs
        - mountPath: /kubeconfig
          name: kubeconfig
        - mountPath: /sfdchosts
          name: sfdchosts
        - mountPath: /config
          name: config
        - mountPath: /data/certs
          name: certs
      hostNetwork: true
      nodeSelector:
        pool: yul-sam
      volumes:
      - hostPath:
          path: /etc/pki_service
        name: maddog-certs
      - hostPath:
          path: /etc/kubernetes
        name: kubeconfig
      - configMap:
          name: sfdchosts
        name: sfdchosts
      - hostPath:
          path: /data/certs
        name: certs
      - configMap:
          name: snapshoter
        name: config
