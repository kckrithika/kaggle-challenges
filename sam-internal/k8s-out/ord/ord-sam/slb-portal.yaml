apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    name: slb-portal
  name: slb-portal
  namespace: sam-system
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: slb-portal
      namespace: sam-system
    spec:
      containers:
      - command:
        - /sdn/slb-portal
        - --hostname=$(NODE_NAME)
        - --templatePath=/sdn/webfiles
        - --port=9112
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        image: ops0-artifactrepo1-0-ord.data.sfdc.net/tnrp/sdn/hypersdn:v-0000736-6a1608d8
        livenessProbe:
          httpGet:
            path: /
            port: 9112
          initialDelaySeconds: 30
          periodSeconds: 3
          timeoutSeconds: 10
        name: slb-portal
        volumeMounts:
        - mountPath: /host/data/slb
          name: var-slb-volume
      nodeSelector:
        pool: ord-sam
      volumes:
      - hostPath:
          path: /data/slb
        name: var-slb-volume
