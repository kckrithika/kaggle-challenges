apiVersion: v1

system:
  functions:
  - name: sip-em
    count: 1
    volumes:
    - name: heroku-secret
      k4aSecret:
        secretName: heroku-api
    - name: blacktab-secret
      k4asecret:
        secretName: blacktab-credentials
    containers:
    - name: sip-em-k4a 
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/sip/em:sip-em-k4a-20190502T1630 
      livenessProbe:
        exec:
          command: ["true"]
      volumeMounts:
      - mountPath: /secrets/heroku/
        name: heroku-secret
        readOnly: true
      - mountPath: /secrets/blacktab/
        name: blacktab-secret
        readOnly: true
