# Manifest Syntax: https://sfdc.co/sam-manifest-format
apiVersion: v1
 
system:
  functions:
  - name: dfs-shared
    count: 1
    containers:
    - name: dfs-shared
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/dfs/service:dfs-0.1.14
      volumeMounts:
      - name: logvol
        mountPath: /home/scone/logs
      - name: dfscert
        mountPath: /certs
      env:
      - name: SFDC_ENVIRONMENT
        value: dfs
      ports:
      - containerPort: 7021
      - containerPort: 15372
      livenessProbe:
        exec:
          command: ["true"]
    - name: sherpa-dfs
      image: sfci/servicelibs/sherpa:2.0.21
      env:
        - name: SFDC_ENVIRONMENT
          value: dfs
      ports:
        - containerPort: 7014
        - containerPort: 7080
      livenessProbe:
        exec:
          command:
          - "./bin/is-alive"
          initialDelaySeconds: 60
          periodSeconds: 120
      readinessProbe:
        exec:
          command:
          - "./bin/is-ready"
          initialDelaySeconds: 60
          periodSeconds: 120
    volumes:
    - name: logvol
      hostPath:
        path: /home/sfdc-dfs/logs
    - name: dfscert
      maddogCert:
        type: client
