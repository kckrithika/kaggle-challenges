apiVersion: v1
system:
  functions:
  - name: pseudo-kubeapi-test
    count: 1
    progressDeadlineSeconds: 120
    containers:
    - name: virtual-api
      command:
        - /sam/virtual-api
        - --sql-db-host=10.251.156.48
        - --sql-db-port=3306
        - --sql-db-name=sdp
        - --sql-db-user=root
        - --sql-db-pass-file=/var/secrets/mysql.txt
        - --sql-db-resources-table=k8s_resource_test
        - --v=4
        - --alsologtostderr
        - --insecure-port=7002
        - --passthrough-api-server=shared0-samdevkubeapi1-1-prd.eng.sfdc.net:40000
        - --passthrough-all-cluster=prd-sam
        - --secret-passthrough-namespace=kube-system
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/d.smith/hypersam:20180306_150100.00f1fa85.dirty.duncsmith-ltm
      ports:
      - containerPort: 7002
      livenessProbe:
        httpGet:
          path: /healthz
          port: 7002
      volumeMounts:
        - mountPath: /var/secrets/
          name: mysql
          readOnly: true
    volumes:
    - name: mysql
      secret:
        secretName: mysql
  loadbalancers:
  - lbname: pseudo-kubeapi-test
    function: pseudo-kubeapi-test
    nodeExposed: true
    ports:
      - port: 40001
        targetport: 7002