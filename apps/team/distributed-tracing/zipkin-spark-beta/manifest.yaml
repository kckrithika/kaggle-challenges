system:
  notifications:
    email:
      - zipkin-scrum-team@salesforce.com
  functions: 
  - name: zipkin-spark-beta
    count: 1
    strategy:
        type: RollingUpdate
        rollingUpdate:
            maxSurge: 0
            maxUnavailable: 1
    containers:
    - name: zipkin-spark-beta
      image: docker-dva-rc/dva/zipkin-dependencies-spark-standalone:13
      resources:
         # To make sure no other apps are deployed on spark host (each has 56 CPU)
        limits: { cpu: "45", memory: "12Gi" }
        requests: { cpu: "45", memory: "12Gi" }
      # See https://github.com/openzipkin/zipkin-dependencies for available ENV options
      env:
      - name: ES_HOSTS
        value: 'shared0-samminionzipkin1-10-prd.eng.sfdc.net,shared0-samminionzipkin1-12-prd.eng.sfdc.net,shared0-samminionzipkin1-13-prd.eng.sfdc.net,shared0-samminionzipkin1-2-prd.eng.sfdc.net,shared0-samminionzipkin1-5-prd.eng.sfdc.net'
      - name: SPARK_USER
        value: ''
      - name: ES_NODES_WAN_ONLY
        value: 'true'
      livenessProbe:
          exec:
              command:
              - /bin/true
          initialDelaySeconds: 5
          periodSeconds: 5