apiVersion: v1

system: 
  functions: 
    - name: na72-search-mario-ord
      type: stateful-set
      count: 15
      containers:  
      - name: searchserver 
        image: sfci/search-teams/sfci:17044869.342
        env:
        - name: SFDC_ENVIRONMENT
          value: "sam"
        - name: INSTANCE_URL
          value: "na72.salesforce.com"
        - name: LBNAME
          value: "searchservers"
        resources:
          limits: { cpu: "48", memory: "90Gi" }
          requests: { cpu: "26", memory: "75Gi" }
        ports:
        - containerPort: 8983
          name: solr-port
        livenessProbe:
          httpGet:
            path: /ping.jsp
            port: 8983
          initialDelaySeconds: 120 
          periodSeconds: 5
        volumeMounts:
          - mountPath: /home/sfdc-searchserver/logs/solr
            name: solr-log-volume 
      lbname: searchservers
      volumes:
        - name: solr-log-volume
          hostPath: 
            path: /home/sfdc-searchserver/logs/solr
        - name: sherpa-log-volume
          hostPath:
            path: /home/sfdc-sherpa/logs