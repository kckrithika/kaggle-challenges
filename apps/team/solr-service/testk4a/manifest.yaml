apiVersion: v1

system:
  functions:
    - name: testk4a
      type: stateful-set
      count: 1
      containers:
      - name: printsecret
        image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/iaas/iaas-buildbase:v1.0.0
        args:
          - -c
          - while true; do ls -R /secrets/; echo contents for the serects folder; find /secrets/ -type f -exec cat {} \; sleep 100;done
        command:
          - /bin/sh
        volumeMounts:
          - mountPath: /secrets/
            name: secretvol
            readOnly: true
      lbname: searchservers
      volumes:
        - name: secretvol
          k4aSecret:
            secretName: testsecret
