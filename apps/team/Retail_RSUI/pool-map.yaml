apps:
  rsui:    # this must match the name of your application (it is case sensitive)
    - prd/prd-sp2-sam_retail_rsui
    - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
  mock-metadata-service:
    - prd/prd-sp2-sam_retail_rsui
    - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
