apiVersion: v1

system:
  notifications:
    email:
      - rpragada@salesforce.com
  functions:
  - name: metrics-publisher-sp-na45-pc1
    count: 1
    containers:
    - name: metrics-publisher
      image: docker-dva-rc/dva/caas/release/caas-metrics-publisher-2.6.12:3
      resources:
        limits: { memory: "2048M" }
        requests: { memory: "1536M" }
      args:
        - --server.port=7514
        - --management.port=7515
        - --cluster.name=sp-na45-pc1
        - --logDir=/home/caas/logs/caas
      volumeMounts:
        - mountPath: /home/caas/logs/caas
          name: logvol
        - mountPath: /var/secrets
          name: secretvol
          readOnly: true
      livenessProbe:
        httpGet:
          path: /manage/config
          port: 7515
        initialDelaySeconds: 60
        timeoutSeconds: 10
      # expose redis outside the container
      ports:
      - containerPort: 7514
      - containerPort: 7515
      env:
       - name: SFDC_ENVIRONMENT
         value: stmfa
       - name: CAAS_ENVIRONMENT
         value: none
       - name: RESTART_COUNT
         value: 1
    volumes:
    - name: logvol
      hostPath:
        path: /home/caas/logs/caas
    - name: secretvol
      secret:
        secretName: caas-secret-sp-na45-pc1
