apiVersion: v1

system:
  notifications:
    email:
      - azeem.mohammad@salesforce.com
  functions:
  - name: devmvp-vagabond-test-client-gcp
    count: 1
    containers:
    - name: vagabond-test-client
      image: ops0-artifactrepo2-0-xrd.slb.sfdc.net/docker-devmvp/vagabond/vagabond-load-test:1
      resources:
        limits: { memory: "6G" }
        requests: { memory: "6G" }
      args:
        - --type=BIGTABLE
        - --url=gsf-core-devmvp-vagabond:vagabond-dev-instance
      livenessProbe:
        exec:
          command: ["true"]
        initialDelaySeconds: 120
        periodSeconds: 5

