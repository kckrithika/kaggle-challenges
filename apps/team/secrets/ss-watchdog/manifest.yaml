apiVersion: v1

system:
  functions:
  # WD monitoring PRD SecretService
  - name: secretservice-watchdog-prd
    count: 1
    containers:
    - name: secretservice-watchdog
      image: dva/secretservice-watchdog:109-1650a4a73bc20fdee9948c8b69b3d721ff5129cf
      command: ["/secretservice/secretservice-watchdog"]
      args:
      - "-ssEndpoint=secretservice-prd.data.sfdc.net"
      - "-wdKingdom=PRD"
      - "-metricsEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80"
      - "-vaultName=ss-canary-vault-prd"
      - "-role=sam.secrets.secretservice-watchdog-prd"
      - "-logtostderr=true"
      - "-enableLifecycleTest=true"
      livenessProbe:
        exec:
          command: ["ls", "/"]
        failureThreshold: 3
        initialDelaySeconds: 5
        periodSeconds: 60
      volumeMounts:
      - mountPath: /clientcert
        name: cert
    volumes:
    - name: cert
      maddogCert:
        type: client
  # WD monitoring production DMZ SecretService
  - name: secretservice-watchdog-crz-from-prd
    count: 1
    containers:
    - name: secretservice-watchdog-crz-from-prd
      image: dva/secretservice-watchdog:107-62ab4da10c572948b5914cb8a1b629412a191b8d
      command: ["/secretservice/secretservice-watchdog"]
      args:
      - "-ssEndpoint=secretservice.dmz.salesforce.com"
      - "-wdKingdom=CRZ"
      - "-metricsEndpoint=ajna0-funnel1-0-prd.data.sfdc.net:80"
      - "-vaultName=ss-canary-vault-crz-from-prd"
      - "-role=sam.secrets.secretservice-watchdog-crz-from-prd"
      - "-logtostderr=true"
      livenessProbe:
        exec:
          command: ["ls", "/"]
        failureThreshold: 3
        initialDelaySeconds: 5
        periodSeconds: 60
      volumeMounts:
      - mountPath: /clientcert
        name: cert
    volumes:
    - name: cert
      maddogCert:
        type: client
