system:
  functions:
  - name: keymaker
    count: 1
    containers:
    - name: keymaker
      image: ops0-artifactrepo2-0-prd.data.sfdc.net/docker-sfci-dev/sfci/ccp/keymaker:24ab5c2
      env:
          - name: HTTPS_PROXY
            value: public0-proxy1-0-frf.data.sfdc.net:8080
          - name: HTTP_PROXY
            value: public0-proxy1-0-frf.data.sfdc.net:8080
          - name: KEYMAKER_SLEEP_SECONDS
            value: 900
          - name: KEYMAKER_TEST
            value: true

      livenessProbe:
        exec:
          command:
            - "true"
        initialDelaySeconds: 120
        periodSeconds: 5

      volumeMounts:
      - mountPath: /client-certs
        name: client-certs
      - mountPath: /server-certs
        name: server-certs

    volumes:
    - name: client-certs
      maddogCert:
        type: client
    - name: server-certs
      maddogCert:
        type: server
