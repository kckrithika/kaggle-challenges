# created by scone-gen e4d02613

apiVersion: v1

system:
  functions:
  - name: datapool-test
    count: 1
    containers:
    - name: datapool-test-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/insights/datapoolservicetest:master-12-8a409c2
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15372
      volumeMounts:
      - mountPath: /home/scone/logs
        name: logvolservice
      - mountPath: /home/datapool-test/client-certs
        name: tls-client-certs
      env:
      - name: SFDC_ENVIRONMENT
        value: kingdom-test

      - name: LOGGING_JSON_STDOUT_LEVEL
        value: all
      - name: LOGGING_STDERR_LEVEL
        value: ALL

      - name: PKI_TRUSTSTORE_DIR
        value: /home/datapool-test/client-certs
      - name: PKI_KEYSTORE_DIR
        value: /home/datapool-test/client-certs/client

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: logvolservice
      hostPath:
        path: /home/sfdc-scone-datapool-test/logs

    - name: tls-server-certs
      maddogCert:
        type: server

    - name: tls-client-certs
      maddogCert:
        type: client
