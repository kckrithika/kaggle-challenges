apiVersion: v1

system:
  functions:
  - name: datapool-engine
    count: 0
    containers:
    - name: datapool-engine
      image: ops0-artifactrepo1-0-ord.data.sfdc.net/dva/datapool-engine:302
      livenessProbe:
        exec:
            command:
            - ls
        initialDelaySeconds: 5
