apiVersion: v1
 
system:
  functions:
  - name: csr-kaiju-master
    count: 1
    containers:
    - name: csr-kaiju-master
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/kviphong/kaiju-master:2
      env:
        - name: ENV
          value: docker
      livenessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        periodSeconds: 60
      readinessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        periodSeconds: 60
      ports:
        - containerPort: 8080

  - name: csr-kaiju-agent
    count: 1
    containers:
    - name: csr-kaiju-agent
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/kviphong/kaiju-agent:4
      env:
        - name: ENV
          value: docker
      livenessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        periodSeconds: 60
      readinessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        periodSeconds: 60
      ports:
        - containerPort: 9090

  loadbalancers:
  - lbname: csrkaijumaster
    function: csr-kaiju-master
    nodeExposed: true
    ports:
    - port: 8081
      targetport: 8080
  - lbname: csrkaijuagent
    function: csr-kaiju-agent
    nodeExposed: true
    ports:
    - port: 9090
      targetport: 9090
