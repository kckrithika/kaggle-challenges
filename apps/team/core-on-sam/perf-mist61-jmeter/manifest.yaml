apiVersion: v1
 
system:
  functions:
  - name: mist61-jmeter
    count: 1
    
    containers:
    - name: mist61-jmeter
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/casam/jmeter:kviphong-20180622-105413

#      resources:
#        limits: { memory: "10240M" }
#        requests: { memory: "8292M" }
 
      livenessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        initialDelaySeconds: 30
        periodSeconds: 120
      readinessProbe:
        exec:
          command:
           - echo
           - HEALTHY
        initialDelaySeconds: 30
        periodSeconds: 120

    strategy:
      type: Recreate
