apiVersion: v1

system:
  functions:
  - name: db-schema-service-worker
    count: 2
    containers:
    - name: db-schema-service-worker
      image: gcr.io/gsf-mgmt-devmvp-spinnaker/dva/db-schema-service-worker:latest
      command: ["java -jar worker/build/libs/worker.jar"]
      env:
        - name: DUMMYENV
          value: "SOMEVALUE"
