apiVersion: v1

system:
  notifications:
    email:
    - servicemesh@salesforce.com
  functions:
  - name: envoy-canary-sandbox
    identity:
      serviceName: envoy-canary
    count: 1
    containers:
    - name: envoy-canary-sherpa
      # https://sconelibci.dop.sfdc.net/job/servicelibs/job/sherpa-envoy/job/master/43
      image: sfci/servicelibs/sherpa-envoy:361a331e5abd22ba41bec4fc3523b6e52dbd8212
      args:
      - --switchboard
      - switchboardsb-lb.service-mesh.prd-sam.prd.slb.sfdc.net:15001
      env:
      - name: SFDC_ENVIRONMENT
        value: mesh
      resources:
        limits: { cpu: "1", memory: "1Gi" }
        requests: { cpu: "1", memory: "1Gi" }
      livenessProbe:
        exec:
          command:
          - ./bin/is-alive
        initialDelaySeconds: 20
        periodSeconds: 5
      readinessProbe:
        exec:
          command:
          - ./bin/is-ready
        initialDelaySeconds: 15
        periodSeconds: 5
      volumeMounts:
      - name: tls-client-cert
        mountPath: /client-certs

    volumes:
    - name: tls-client-cert
      maddogCert:
        type: client
