# This app runs with istio-proxy getting configs from Istio Pilot. Note the annotation "sidecar.istio.io/inject".
# Ordering (istio-proxy) -> Shipping (istio-proxy).

apiVersion: v1

system:
  functions:
  - name: istio-shipping
    count: 1
    labels:
      app: istio-shipping
      settings_path: istio.-.prd.-.istio-shipping
      istio_app: test
      chaos.sfdc.net/podDelete: true
      chaos.sfdc.net/podDeletePeriod: 4h
    annotations:
      sidecar.istio.io/inject: "true"
    containers:
    - name: shipping
      # Shipping App
      #   https://git.soma.salesforce.com/services/sfdc-bazel/tree/master/projects/services/shipping/shipping-scone
      # Shipping Config
      #   https://git.soma.salesforce.com/services/sfdc-bazel/blob/master/projects/services/shipping/shipping-scone/src/main/resources/config/application.properties
      # Image build:
      # https://sfcirelease.dop.sfdc.net/job/services/job/sfdc-bazel_projects_services_examples_shipping/job/sfdc-bazel/job/master/73/
      image: sfci/services/sfdc-bazel/scone/shipping:b51d7f786
      env:
      - name: SFDC_ENVIRONMENT
        value: istio
      - name: GRPC_PORT
        value: 7443
      - name: JAVA_OPTS
        value: "-Dtracing.report=false -Dmesh.istio-mode=true -Dscone.service.name=istio-shipping -Dmanagement.server.port=15372"
      - name: JAVA_HEAP_OPTS
        value: -Xmx64m
      ports:
      - containerPort: 7443
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 15
        httpGet:
          path: /manage/health
          port: 15372

    # Volumes used by auto-injected istio-proxy.
    volumes:
    - name: tls-client-cert
      maddogCert:
        type: client
    - name: tls-server-cert
      maddogCert:
        type: server

  loadbalancers:
    - lbname: istio-shipping
      function: istio-shipping
      annotations:
        routing.mesh.sfdc.net/enabled: "true"
      labels:
        app: istio-shipping
        settings_path: istio.-.prd.-.istio-shipping
      ports:
        - lbtype: dsr
          nodeExposed: false
          port: 7443
          targetport: 7443
          name: grpc-shipping
