apiVersion: v1

system:
  functions:
  - name: jenkins-tnrp-master
    count: 1
    containers:
    - name: jenkins-tnrp-master
      image: tnrp/jenkins-master:2.32.2
      ports:
      - containerPort: 8080
      - containerPort: 50000
      resources:
        limits: { cpu: "10", memory: "20Gi" }
        requests: { cpu: "10", memory: "20Gi" }
      volumeMounts:
        - mountPath: /var/jenkins_home
          name: jenkins-data
      livenessProbe:
        httpGet:
          path: /
          port: 8080
        initialDelaySeconds: 5
    volumes:
      - name: jenkins-data
        emptyDir: {}
  loadbalancers:
  - lbname: jenkinstnrpmasterlb
    function: jenkins-tnrp-master
    nodeExposed: true
    ports:
    - port: 8080
      targetport: 8080
