# created by scone-gen 3bd9a4697

apiVersion: v1

system:
  functions:
  - name: expression-evaluator
    count: 2
    containers:
    - name: expression-evaluator-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/gater/expression-evaluator:d1180ea4b
      args:
      - --noop.arg=123
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15372
      readinessProbe:
        timeoutSeconds: 10
        httpGet:
          path: /manage/ready
          port: 15372

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: logvolservice
      hostPath:
        path: /home/sfdc-scone-expression-evaluator/logs
  loadbalancers:
  - lbname: expression-evaluator-lb
    function: expression-evaluator
    slbEnabled: true
    ports:
    - lbtype: http
      port: 80
      targetport: 8080
    - lbtype: http
      port: 15372
      targetport: 15372
