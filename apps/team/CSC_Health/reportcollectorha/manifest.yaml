apiVersion: v1

system:
  functions:
  - name: report-collectorha
    count: 3
    containers:
    - name: report-collectorha
      image: report-collector:1.3.0-20161114T233958
      ports:
      - name: provider
        containerPort: 8080
      - name: consumer
        containerPort: 8081
      - name: provider-https
        containerPort: 8443
      - name: consumer-https
        containerPort: 8444
      resources:
        limits: { cpu: "10", memory: "20Gi" }
        requests: { cpu: "10", memory: "20Gi" }
      logs:
      - directory: /var/log/report-collector
      env:
      - name: ReportCollector_redis_host
        value: $(REDISNOHALB_SERVICE_HOST)
      - name: ReportCollector_app_https_enabled
        value: true
      - name: ReportCollector_ajna_publishEnabled
        value: true
      command: ["/bin/sh", "-c"]
      args: ["
      export ReportCollector_ajna_funnelHost=${SFDC_METRICS_SERVICE_HOST} ;
      export ReportCollector_ajna_funnelPort=${SFDC_METRICS_SERVICE_PORT} ;
      export ReportCollector_ajna_datacenter=${KINGDOM} ;
      export ReportCollector_ajna_device=${FUNCTION_INSTANCE_NAME} ;
      ${JAVA_HOME}/bin/java
        -Dlogback.configurationFile=${HOME}/logback-docker.xml
        -Dvertx.logger-delegate-factory-class-name=io.vertx.core.logging.SLF4JLogDelegateFactory
        -Dvertx.metrics.options.enabled=true
        -Dvertx.metrics.options.registryName=vertx-registry
        -Dvertx.disableDnsResolver=true
        -Dlog_dir=/var/log/report-collector
        -jar /usr/lib/report-collector/report-collector.jar
      "]
  - name: redisnoha
    count: 1
    containers:
    - name: redis
      image: rc-redis:3.0
      ports:
      - containerPort: 6379
      resources:
        limits: { cpu: "2", memory: "10Gi" }
        requests: { cpu: "2", memory: "10Gi" }
      logs:
      - directory: /var/log/redis

  loadbalancers:
  - lbname: reportcollectorhalb
    function: report-collectorha
    nodeExposed: true
    ports:
      # LB was supposed to allow SSL on the backend, but its not currently
      # supported, so we will use non SSL but on these node ports (18443, 18444)
      # since its the one exposed in estates for sam_minionreportcollector
    - port: 18443
      targetport: 8080
    - port: 18444
      targetport: 8081
  - lbname: redisnohalb
    function: redisnoha
    nodeExposed: true
    ports:
      - port: 6379
        targetport: 6379
