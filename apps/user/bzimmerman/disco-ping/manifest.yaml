# created by scone-gen 9c53116

apiVersion: v1

system:
  functions:
  - name: disco-ping
    count: 1
    containers:
    - name: disco-ping-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/bzimmerman/disco-ping:bzimmerman-20180706-b91dad2
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15372
      volumeMounts:
      - mountPath: /home/scone/logs
        name: logvolservice
    - name: beacon-disco-ping
      # https://sconelibci.dop.sfdc.net/job/servicelibs/job/beacon/job/master/
      image: sfci/servicelibs/beacon:853c4db9f14805018be6f5e7607ffe65b5648822
      # This announces your gRPC endpoint for HTTP2 traffic without mTLS.
      # See https://git.soma.salesforce.com/servicelibs/sherpa#ports
      args:
      - -endpoint
      - disco-ping:DATACENTER:7012
      - -logging.dir
      - /log
      livenessProbe:
        initialDelaySeconds: 5
        exec:
          command:
            - "/home/scone/is-alive.sh"
      # Need to mount to a volume so that logs can be shipped to splunk
      volumeMounts:
      - mountPath: /log
        name: logvolbeacon
    - name: sherpa-disco-ping
      # https://git.soma.salesforce.com/servicelibs/sherpa-envoy
      image: sfci/servicelibs/sherpa-envoy:71a494bfe38cee2949ce3c57ef0545e6f03710e1
      # See https://git.soma.salesforce.com/servicelibs/sherpa#ports
      ports:
      - containerPort: 15373
        name: sherpa-adm
      - containerPort: 7012
        name: grpc-in
      livenessProbe:
        exec:
          command:
          - "./bin/is-alive"
        initialDelaySeconds: 120
        periodSeconds: 5
      readinessProbe:
        exec:
          command:
          - "./bin/is-ready"
        initialDelaySeconds: 110
        periodSeconds: 5

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: logvolbeacon
      hostPath:
        path: /home/sfdc-scone-disco-ping-beacon/logs
    - name: logvolservice
      hostPath:
        path: /home/sfdc-scone-disco-ping/logs
