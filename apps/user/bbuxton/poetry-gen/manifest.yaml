# created by scone-gen a500d29a

apiVersion: v1

system:
  functions:
  - name: poetry-gen
    count: 1
    containers:
    - name: poetry-gen-service
      # latest image to trigger a new build
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/bbuxton/poetry-gen:bbuxton-20190322-latest
      command: ["/bin/sh"]
      args: ["-c", "sleep 10 && echo Sleep expired"]
      terminationMessagePolicy: "FallbackToLogsOnError"
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15373
      volumeMounts:
      - mountPath: /home/scone/logs
        name: logvolservice
    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: logvolservice
      hostPath:
        path: /home/sfdc-scone-poetry-gen/logs

