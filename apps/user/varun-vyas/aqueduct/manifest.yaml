apiVersion: v1

system:
  functions:
    - name: aqueduct
      count: 1
      hostnetwork: true
      containers:
      - name: aqueduct
        image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/varun/aqueduct:aqueduct_scone4.1.2 # Use the latest version, see below
        env:
        - name: SFDC_ENVIRONMENT
          value: "sam" # Use whatever environment your kpod uses
        ports:
        - containerPort: 25674
          name: ext-port
        - containerPort: 25675
          name: inter-port
        - containerPort: 45699
          name: manage-port
        livenessProbe:
          initialDelaySeconds: 500
          periodSeconds: 30
          timeoutSeconds: 3600
          httpGet:
            path: /available
            port: 25674
            scheme: HTTP

# loadbalancers:
#   - lbname: aqueduct
#     function: default
#     slbEnabled: true
#     ports:
#      - port : 8080
#        targetport : 8080
#        lbtype : <tcp|http|dsr>
