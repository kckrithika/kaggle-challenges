apiVersion: v1

system:
  functions:
  - name: us-xlt-irahim
    count: 9
    containers:
    - name: us-xlt-irahim
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/irahim/xlt-agent:20171010
      ports:
      - containerPort: 8500
        name: us-xlt-port
      livenessProbe:
        exec:
          command:
          - cat
          - ./bin/agentcontroller.sh
      readinessProbe:
        exec:
          command:
          - cat
          - ./bin/agentcontroller.sh
    - name: sherpa-xlt
      image: tnrp/sherpa:0.2.0-0000097-594964a3
      env:
      - name: SFDC_ENVIRONMENT
        value: "sam"
      resources:
        limits: { cpu: "1", memory: "1Gi" }
        requests: { cpu: "1", memory: "1Gi" }
      ports:
      - containerPort: 7080
        name: sherpa-xlt-adm
      livenessProbe:
        exec:
          command:
          - "./bin/is-alive"
      readinessProbe:
        exec:
          command:
          - "./bin/is-ready"
