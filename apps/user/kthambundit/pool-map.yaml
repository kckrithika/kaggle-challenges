version: v2
apps:
 my-sam-test:    # this must match the name of your application. It must be lower case.
  - name: prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 gid-runtime-service:
  - name: xrd/xrd-sampool