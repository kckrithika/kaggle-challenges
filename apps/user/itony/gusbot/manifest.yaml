# created by scone-gen af488301c

apiVersion: v1

system:
  functions:
  - name: gusbot
    count: 1
    containers:
    - name: gusbot-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/itony/gusbot:1
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        exec:
          command:
          - cat
          - /tmp/live
        initialDelaySeconds: 10
        periodSeconds: 5

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: cert
      maddogCert:
        type: client
        
  loadbalancers:
    - lbname: gusbot
      function: gusbot
      nodeExposed: false
      slbenabled: true
      ports:
        - port: 8080
          targetport: 8080
          lbtype: http
          #tls: true
          #reencrypt: true
        - port: 15372
          targetport: 15372
          lbtype: http
          #tls: true
          #reencrypt: true