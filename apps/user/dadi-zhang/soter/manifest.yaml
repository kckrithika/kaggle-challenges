apiVersion: v1
system:
  functions:
  - name: default
    count: 1
    containers:
    - name: soter
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/dadi-zhang/dadi-soter:test
      ports:
      - containerPort: 9090
      livenessProbe:
        exec:
          command:
          - ls
        initialDelaySeconds: 5
        periodSeconds: 5
