apps:
 mgmt-services:    # this must match the name of your application (it is case sensitive)
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
 mgmt-console:    # this must match the name of your application (it is case sensitive)
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
