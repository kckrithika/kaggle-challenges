apiVersion: v1

system:
  functions:
  - name: shipping-istio
    count: 1
    labels:
      disco_dc: "PRD"
      disco_env: ""
      disco_subenv: ""
      disco_pod: ""
      disco_sp: ""
    annotations:
      sidecar.istio.io/inject: "true"
    containers:
    - name: shipping
      # Shipping App
      #   https://git.soma.salesforce.com/services/sfdc-bazel/tree/master/projects/services/shipping/shipping-scone
      # Shipping Config
      #   https://git.soma.salesforce.com/services/sfdc-bazel/blob/master/projects/services/shipping/shipping-scone/src/main/resources/config/application.properties
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/scone/shipping-service:bazel-monorepo
      env:
      - name: SFDC_ENVIRONMENT
        value: istio
      - name: JAVA_HEAP_OPTS
        value: -Xmx64m
      - name: GRPC_PORT
        value: 7035
      ports:
      - containerPort: 7443 # This would be added in the istio-proxy container.
        name: grpc-proxy
      - containerPort: 7035
        name: grpc-svc
      - containerPort: 15375
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 15
        httpGet:
          path: /manage/health
          port: 15375
    # Volumes used by auto-injected istio-proxy.
    volumes:
    - name: tls-client-cert
      maddogCert:
        type: client
    - name: tls-server-cert
      maddogCert:
        type: server
