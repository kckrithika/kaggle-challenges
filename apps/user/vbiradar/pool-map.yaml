apps:
 sam-hello-world:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
 mgmt-db:
  - prd/prd-sam
 mgmt-api:
  - prd/prd-sam
 mgmt-ui:
  - prd/prd-sam
 mgmt-pd:
  - prd/prd-sam

