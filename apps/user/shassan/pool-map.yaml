apps:
 my-sam-test:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 my-sam-forexservice:
  - prd/prd-sam
 my-sam-currencyservice:
  - prd/prd-sam
 sam-spydrservice:
  - prd/prd-sam
