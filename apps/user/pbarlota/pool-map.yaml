apps:
 sam-c2c-jmh-perftestingapp-servicemesh:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-coreproxy:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-coreproxy:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-sme-servicemesh:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-slb-capacity:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-slb-capacity-1:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-slb-capacity-2:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
 sam-c2c-jmh-perftestingapp-c2cservice-slb-capacity-4:    # this must match the name of your application. It must be lower case.
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools

