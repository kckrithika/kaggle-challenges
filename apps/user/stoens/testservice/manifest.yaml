# created by scone-gen 6926910

apiVersion: v1

system:
  functions:
  - name: testservice
    count: 1
    containers:
    - name: testservice-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/stoens/testservice:stoens-20180712-353bf5b
      ports:
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15372
      volumeMounts:
      - mountPath: /home/scone/logs
        name: logvolservice

    - name: beacon
      #https://sconelibci.dop.sfdc.net/job/servicelibs/job/beacon/job/master/230/console
      image: sfci/servicelibs/beacon:0078716f45375dc0b114849e13d9b1dbf83177a9
      args:
      - -endpoint
      - foo1234:DATACENTER:1234
      livenessProbe:
        initialDelaySeconds: 5
        exec:
          command:
            - "/home/scone/is-alive.sh"
      # Need to mount to a volume so that logs can be shipped to splunk
      volumeMounts:
      - mountPath: /home/scone/logs
        name: logvolbeacon

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
    - name: logvolservice
      hostPath:
        path: /home/sfdc-scone-testservice/logs
    - name: logvolbeacon
      hostPath:
        path: /home/sfdc-scone-testservice-beacon/logs
