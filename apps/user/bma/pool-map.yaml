apps:
     # this must match the name of your application. It must be lower case.
     # the SAM Sandbox uses the prd/prd-sam host cluster
     # you can also specify other pools
 message-rendition-service:    
  - prd/prd-sam