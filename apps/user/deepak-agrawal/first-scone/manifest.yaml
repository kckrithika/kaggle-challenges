# created by scone-gen 59a14645

apiVersion: v1

system:
  functions:
  - name: first-scone
    count: 1
    containers:
    - name: first-scone-service
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/deepak.agrawal/first-scone:deepak.agrawal-20190506-080c040 
      # If your service doesn't serve HTTP traffic then you can remove the scone-http 8080 port.
      ports:
      - containerPort: 8080
        name: scone-http
      - containerPort: 7020
        name: grpc-svc
      - containerPort: 15372
        name: scone-mgmt
      livenessProbe:
        initialDelaySeconds: 30
        httpGet:
          path: /manage/health
          port: 15372
      readinessProbe:
        timeoutSeconds: 10
        httpGet:
          path: /manage/ready
          port: 15372

    # The volumes to mount for shipping logs to splunk
    # General rule is to use path that satisfies the pattern: /home/sfdc-*/logs/**/*.json
    volumes:
