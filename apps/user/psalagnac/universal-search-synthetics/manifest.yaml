apiVersion: v1

system:
  notifications:
    email:
    - psalagnac@salesforce.com
  functions:
  - name: universal-search-synthetics
    count: 1
    volumes:
    - name: synthetics-log-vol
      hostpath:
        path: /home/sfdc-usmt/logs
    - name: client-cert-vol
      maddogCert:
        type: client
    - name: sherpa-log-vol
      hostpath:
        path: /home/sfdc-sherpa/logs
    containers:
    - name: universal-search-synthetics
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/psalagnac/universal-search-synthetics:pierre1
      env:
      - name: SFDC_ENVIRONMENT
        value: "stmfa"
      resources:
        limits: { cpu: "1", memory: "1Gi" }
        requests: { cpu: "1", memory: "1Gi" }
      volumeMounts:
        - name: synthetics-log-vol
          mountPath: /home/sfdc-usmt/logs
      livenessProbe:
        exec:
          command: ["true"]
      readinessProbe:
        exec:
          command:
          - "/home/sfdc-usmt/run.sh --domain synthetics-shared"
        initialDelaySeconds: 60
        timeoutSeconds: 30
        periodSeconds: 900
        failureThreshold: 1
    - name: universal-search-synthetics-cre
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/psalagnac/universal-search-synthetics:pierre1
      env:
      - name: SFDC_ENVIRONMENT
        value: "stmfa"
      resources:
        limits: { cpu: "1", memory: "1Gi" }
        requests: { cpu: "1", memory: "1Gi" }
      volumeMounts:
        - name: synthetics-log-vol
          mountPath: /home/sfdc-usmt/logs
      livenessProbe:
        exec:
          command: ["true"]
      readinessProbe:
        exec:
          command:
          - "/home/sfdc-usmt/run.sh --domain synthetics-cre"
        initialDelaySeconds: 60
        timeoutSeconds: 30
        periodSeconds: 900
        failureThreshold: 1
