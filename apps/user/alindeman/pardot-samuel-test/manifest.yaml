apiVersion: v1

system:
  functions:
  - name: default
    count: 1
    containers:
    - name: samuel
      image: shared0-samcontrol1-1-prd.eng.sfdc.net:5000/pardot-samuel:0.2
      ports:
      - containerPort: 3000
      livenessProbe:
        httpGet:
          path: /
          port: 3000

  loadbalancers:
  - lbname: samuel
    function: default
    ports:
      - port: 80
        targetport: 3000
