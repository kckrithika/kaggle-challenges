apiVersion: v1
 
system:
  functions:
  - name: hello
    count: 1

    containers:
    - name: helloc
      image: ops0-artifactrepo1-0-prd.data.sfdc.net/docker-sam/mgainsborough/hello:1
 
      livenessProbe:
        exec:
          command:
           - echo
           - HEALTHY

      readinessProbe:
        exec:
          command:
           - echo
           - HEALTHY

      ports:
        - containerPort: 8800
        
  loadbalancers:
  - lbname: hellolb
    function: hello
    nodeExposed: true
    ports:
    - port: 18800
      targetport: 8800
