apiVersion: v1
 
system:
  notifications:
    email:
      - emailinfrastructure@salesforce.com
  functions:
  - name: eaas
    count: 3 
    strategy:
      type: RollingUpdate
    containers:
    - name: eaas
      image: docker-sfci-dev/sfci/pkonieczny/emailservice:fa220522fc9ed4f7d6b5279546c7cccb05e3eb34
      env:
      - name: SFDC_ENVIRONMENT
        value: "sam"
      - name: GRPC_ANNOUNCED_PORT
        value: 7012
      - name: SCONE_SERVICE_NAME
        value: eaas
      ports:
        - name: grpc
          containerPort: 7020
      volumeMounts:
      - mountPath: /home/sfdc-eaas/logs
        name: logvol
      livenessProbe:
        httpGet:
          path: /manage/health
          port: 15372
        initialDelaySeconds: 120
        periodSeconds: 60
        timeoutSeconds: 30
      readinessProbe:
        httpGet:
          path: /manage/health
          port: 15372
        initialDelaySeconds: 110
        periodSeconds: 60
        timeoutSeconds: 30

    - name: sherpa-eaas
      image: sfci/servicelibs/sherpa:2.0.21
      args: ["--zipkin-rate", "0.0"]
      env:
      - name: SFDC_ENVIRONMENT
        value: "sam"
      resources:
        limits: { cpu: "1", memory: "1Gi" }
        requests: { cpu: "1", memory: "1Gi" }
      ports:
      - containerPort: 7012
        name: sherpa-eaas-in
      - containerPort: 7443
        name: sherpa-eaas-tls
      - containerPort: 15373
        name: sherpa-eaas-adm
      livenessProbe:
        exec:
          command:
          - cat
          - ./output/config.yaml
        initialDelaySeconds: 120
        periodSeconds: 50
      readinessProbe:
        exec:
          command:
          - cat
          - ./output/config.yaml
        initialDelaySeconds: 110
        periodSeconds: 50
      volumeMounts:
      - name: eaas-server-cert
        mountPath: /server-certs

    volumes:
    - name: logvol
      hostPath:
        path: /home/sfdc-eaas/logs
    - name: eaas-server-cert
      maddogCert:
        type: server
