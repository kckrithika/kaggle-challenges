apps:
  capadd:    # this must match the name of your application. It must be lower case.
    - prd/prd-sam  # Stale app disabled by automation.  See https://sfdc.co/samsandbox
     # the SAM Sandbox uses the prd/prd-sam host cluster
                 # you can also specify other pools
