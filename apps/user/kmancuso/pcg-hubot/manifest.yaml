---
apiVersion: v1

system:
  functions:
  - name: pcg-hubot
    count: 1
    containers:
    - name: pcg-hubot
      image: ops0-artifactrepo2-0-prd.data.sfdc.net/dva/pcg-mrpotatobot:30
      command: ["/opt/mrpotatobot/run.sh"]
      ports:
        - containerPort: 8080
      livenessProbe:
        exec:
          command: ["true"]
        initialDelaySeconds: 60
        periodSeconds: 5
      env:
      - name: HTTP_PROXY
        value: public0-proxy1-0-prd.data.sfdc.net:8080
      - name: HTTPS_PROXY
        value: public0-proxy1-0-prd.data.sfdc.net:8080
      - name: PLACEBO
        value: 0002
      volumeMounts:
      - mountPath: /secrets/
        name: hubot
        readOnly: true
    volumes:
    - name: hubot
      k4aSecret:
        secretName: hubot

  loadbalancers:
  - lbname: pcg-hubot
    function: pcg-hubot
    slbenabled: true
    ports:
      - port: 443
        targetport: 8080
        lbtype: http
        tls: true
