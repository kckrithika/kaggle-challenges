apps:
 ceph-test-IO:    # this must match the name of your application (the folder you created above). It is case sensitive
  - prd/prd-sam  # the SAM Sandbox uses the prd/prd-sam host cluster
 
                 # you can also specify other pools
